import { WithAnchorDirective } from './with-anchor.directive';

describe('WithAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new WithAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
